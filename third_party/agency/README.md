# Selected upstream source

Original source and license retained unchanged. Exact commit and hashes are in manifest.json. Fehm adaptations are identified in src/upstream-adaptations.ts and src/review-freshness.ts. These are selected components, not installation of the complete upstream product.

Original files live under vendor/ so Fehm’s existing dependency-directory exclusion prevents these snapshots from entering its own source graph.
